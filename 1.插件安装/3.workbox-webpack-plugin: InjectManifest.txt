介绍：工具库，提供了一系列对 Service Work 操作的 API，基本覆盖大部分Service Worker 场景

部署：需要增加一个 sw.js 的路由

优点：
  1. 自定义配置较强，几乎覆盖全部场景
  2. Webpack 插件编译使得利用import 引入workbox，在编译后的注册文件中包含worbox的源代码，无需下载cdn链接
  3. 自动生成__WB_MANIFEST变量，是编译后静态文件链接的数组，可以以此为基础进行预加载。

缺点：
  1.SPA无明显缺点
  2.但是在现在常用的微前端框架下，主应用和子应用都需要分别配置，开发量较高